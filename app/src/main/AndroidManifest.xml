<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.recotapsdk.sdkrecotap">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="RECOTAP_ACCOUNT_ID"
            android:value="Something1234" />

        <meta-data
            android:name="RECOTAP_CLIENT_ID"
            android:value="01U11SeHvN1JuqI" />

        <meta-data
            android:name="RECOTAP_CLIENT_SECRET"
            android:value="WZdoawHBBjjtWQ6mIiYpYLk2C" />

        <meta-data
            android:name="RECOTAP_TOKEN_URL"
            android:value="https://testapi.recotap.com/v1/oauth/token?key=CC7LyIcNj5VYpTjW8n85fuFFkv7Ygg" />

        <meta-data
            android:name="RECOTAP_ENDPOINT_URL"
            android:value="https://testapi.recotap.com/v1/api/events" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>